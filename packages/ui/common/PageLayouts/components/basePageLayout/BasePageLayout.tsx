import React from 'react';
import { Header } from '../../../../shell/src/view/components';

const BasePageLayout: React.FC = () => (
  <section>
    <Header />
    <main></main>
  </section>
);

export default BasePageLayout;
